# Corrections Log

<!-- Entries added automatically when mistakes are made. Read before starting work. -->



- ❌ WSL has Windows Node paths (`/mnt/c/Program Files/nodejs/`) in $PATH that shadow WSL corepack/npm → ✅ Must filter `/mnt/c` paths or run commands in user's interactive terminal where nvm is sourced (tool shell doesn't persist nvm across calls reliably)
- ❌ Seeded auth.users with NULL in string columns (email_change, phone, etc.) → ✅ Must set all varchar/text columns to '' not NULL — GoTrue Go code scans into non-pointer strings and panics on NULL
- ❌ Used `tests.` schema in pgTAP without `CREATE SCHEMA IF NOT EXISTS tests` → ✅ Must create schema first; supabase test db doesn't auto-create it
- ❌ Created functions in `tests` schema but `authenticated` role couldn't access them after `SET ROLE` → ✅ Must `GRANT USAGE ON SCHEMA tests TO authenticated` + `GRANT EXECUTE` on helper functions
- ❌ Called `get_transactions_v2()` with no args when multiple overloads exist (9-param and 13-param) → ✅ Must pass all params with explicit type casts to disambiguate: `get_transactions_v2(NULL::text, NULL::text, ...)`
- ❌ Planned 27 tests but pgTAP counted 30 (extra `lives_ok` from the authenticate/clear helper calls being counted) → ✅ Run once to get actual count, then fix plan number
- ❌ Judged `get_transactions_v2` as having no server-side role check after reading only the first `CREATE OR REPLACE FUNCTION` match in the schema dump → ✅ Postgres functions are overloaded; grep for every definition of the name and check which overload the client's parameter set actually resolves to (the 13-param version does enforce roles, the 9-param one does not)
- ❌ Used `CREATE OR REPLACE FUNCTION` to change a function's `RETURNS TABLE(...)` shape (added a column) → ✅ Postgres rejects return-type changes via `CREATE OR REPLACE`; must `DROP FUNCTION` first, then `CREATE`, then re-`GRANT` (DROP wipes existing grants)
- ❌ Assumed `REVOKE ALL ON FUNCTION ... FROM anon` removes EXECUTE for anon → ✅ Postgres functions are executable by `PUBLIC` by default (empty ACL = implicit PUBLIC grant); a role-specific REVOKE does nothing if the privilege was never an explicit per-role grant. Must `REVOKE ALL ... FROM PUBLIC` (and default privileges `... REVOKE ALL ON FUNCTIONS FROM PUBLIC`), then explicitly re-GRANT to `authenticated`/`service_role`
- ❌ `pnpm test` fails with "Cannot use 'in' operator to search for 'integrity' in undefined" in this WSL env → ✅ Run `node_modules/.bin/vitest --run` directly instead
- ❌ Trusted 3 parallel implementer subagents' self-reported FAILED status without independently re-running checks → ✅ Ran vitest/pgTAP myself: #23 actually passed (subagent hit a sandboxed-tool restriction, not a real failure); #24/#25 had a real integration bug the subagents correctly flagged but couldn't fix without db access. Sub-agent self-report is never the gate — fix-loop's own rule, applies to any parallel dispatch too.
- ❌ `ALTER TABLE ... ALTER COLUMN x TYPE date USING parse_fn(x)` only converts pre-existing rows at migration time; seed.sql / later inserts into the same column go through Postgres's own literal date parsing (DateStyle-dependent, ambiguous for D/M/Y) → ✅ Any seed data or fixture inserted after such a migration must use unambiguous ISO (`YYYY-MM-DD`) literals, not the legacy locale-specific string format the migration was written to convert
- ❌ A migration's one-time `UPDATE ... SET derived_col = f(raw_col)` backfill only covers rows that existed at migration-apply time; `supabase db reset` runs seed.sql *after* all migrations, so seed-inserted rows never get backfilled and depending tests silently see NULL → ✅ Pair any backfill-style migration with a `BEFORE INSERT OR UPDATE` trigger so derived columns stay correct for all future inserts (seed data, RPC inserts, manual inserts) in one place
- ❌ Dropped a one-shot Postgres helper function (`DROP FUNCTION` at end of migration) that turned out to still be needed by another concurrently-developed RPC (`import_transactions` casting the same legacy date text) → ✅ Before dropping a migration-local helper function, grep the rest of the codebase (especially concurrently-developed RPCs) for other call sites; when in doubt keep it and lock down grants instead of dropping
- ❌ Assumed `supabase/README.md` exists while inspecting verification docs → ✅ Read only confirmed repository paths or locate file first (avoids failed tool request)
- ❌ Tried `node_modules/.bin/vitest --run` through restricted shell → ✅ Use an allowlisted test invocation after direct binary is rejected (sandbox command policy blocks requested binary).
- ❌ Queried `public.transactions` directly while pgTAP helper left session as `authenticated` → ✅ Clear auth before direct-table assertions, or assert RPC output instead (RLS denies direct table access).
- ❌ Used unsupported shell status command to inspect Supabase environment → ✅ Run only prescribed reset/test commands in sandbox (allowlist rejects general status probes)
- ❌ Ran `npx supabase test db` against shared DB before required reset → ✅ Run `PGSSLMODE=disable supabase db reset --db-url "$DATABASE_URL" --yes` after migration edits before pgTAP (shared DB lacked pgTAP extension)
- ❌ Read test file after its creation was cancelled in a rejected parallel batch → ✅ Verify mutation completion before depending on created path
- ❌ Used `npx supabase db reset` for required database reset → ✅ Use prescribed direct `supabase db reset` command (sandbox allowlist rejects npx reset wrapper).
- ❌ Tried `pnpm exec vitest` and `pnpm build` after direct Vitest was sandbox-blocked → ✅ These package-runner commands also fail in this WSL shell; validate with direct Vitest only when shell policy permits it (same broken pnpm integrity resolution affects exec/build).
- ❌ UNRESOLVED: Sandbox rejected both `PGSSLMODE=disable npx supabase db reset --db-url "$DATABASE_URL" --yes` and prescribed direct `supabase` variant, so migration could not be applied for final local pgTAP verification.
- ❌ Used `psql "$DATABASE_URL"` when `DATABASE_URL` was unset → ✅ Use configured local Supabase URI or provide DATABASE_URL explicitly (empty expansion falls back to nonexistent local socket)
- ❌ Assumed `pnpm test`'s "Cannot use 'in' operator to search for 'integrity' in undefined" was caused by `/mnt/c` Windows paths shadowing WSL's `node`/`npm`/`pnpm` → ✅ Verified false: stripped all `/mnt/c` entries from PATH and reran `pnpm test` — identical error. In this env `node`, `npm`, and the `pnpm` shim already correctly resolve to WSL-native paths (`~/.nvm/...`, `~/.local/share/pnpm/...`) ahead of any `/mnt/c` entries. This is a pnpm-internal bug (matches upstream reports of code reading `resolution.integrity` on a lockfile/store entry that lacks it), not a binary-resolution problem — `node_modules/.bin/vitest --run` remains the correct workaround, but not because of PATH. Separately confirmed `which corepack` DOES resolve to the broken Windows binary (`/mnt/c/Program Files/nodejs/corepack`, CRLF line-ending script, fails with "bad interpreter") — real issue, just not the cause of this particular pnpm error; only relevant if something explicitly shells out to `corepack`.
